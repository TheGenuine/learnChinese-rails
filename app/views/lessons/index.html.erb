  <div class="row">
  <%= @lessons.each do |lesson|%>
        <div class="col-lg-4">
          <h2><%= lesson.title %></h2>
          <p><%= lesson.description %></p>
          <p><%= link_to "Details", lessons_show_path, :class => "btn btn-primary"%></p>
        </div>
  <% end %>
</div>